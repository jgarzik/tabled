This is tabled, the distributed lookup table daemon.

(though currently non-distributed)

Current dependencies:
- GLib 2.x.x
- pcre
- sqlite 3.x
- openssl
- db4 4.x

Only tested on Linux, though portability patches are welcome.

See doc/ subdirectory for more information.
