#!/bin/sh

DATADIR=data
TDBDIR=$DATADIR/tdb

mkdir -p $TDBDIR

cat $top_srcdir/test/users.data | ../server/tdbadm -u -t $TDBDIR

if [ ! -f $TDBDIR/passwd ]
then
	rm -rf $DATADIR
	echo "test database not found."
	exit 1
fi

exit 0
