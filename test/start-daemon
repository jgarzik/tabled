#!/bin/sh

if [ -f tabled.pid ]
then
	echo "pid file found.  daemon still running?"
	exit 1
fi

cat <<EOF >tabled.conf
<PID>tabled.pid</PID>
<Listen><Port>18080</Port></Listen>
<Data>data</Data>
<TDB>data/tdb</TDB>
EOF

../server/tabled -C tabled.conf

sleep 3

exit 0
